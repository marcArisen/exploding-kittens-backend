// file for experiment

// const timeout = new Promise((resolve) => setTimeout(resolve, 5000));

// const response = await Promise.race([this.actionCallBack, timeout]);
// if (response) {
//             return response;
//         }
// console.log(timeout);

// setTimeout(show, 6000);

// function show(){
//     console.log(timeout)
// }
